# TODO(turf): replace with the Turf Supabase project ID once the project is created.
# Get this from Supabase dashboard → Project Settings → General → Reference ID.
project_id = "REPLACE_WITH_TURF_SUPABASE_PROJECT_ID"